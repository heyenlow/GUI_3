<Page
    x:Class="GUI_3.ResultsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUI_3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="30" Padding="10" BorderBrush="Red"  BorderThickness="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Great Job!" FontSize="42" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10"/>
        <TextBlock Text="{x:Bind resultsString}" FontSize="36" HorizontalAlignment="Center" Grid.Row="1"/>

    </Grid>
</Page>
